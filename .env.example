# Copy to .env.local for local work. Never commit real values.
PORT=3000
# ipinfo.io token for "What is my IP" and "IP lookup". Without it those tools say so.
IPINFO_TOKEN=
# GA4: measurement id for the page tag, API secret for server-side MCP events. Both optional.
GA_MEASUREMENT_ID=
GA_API_SECRET=
